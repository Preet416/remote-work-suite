{
  "name": "server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build-client": "cd ../client && npm install && npm run build && rm -rf ../server/build && cp -r build ../server/",
    "build": "npm run build-client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "socket.io": "^4.8.1",
    "path": "^0.12.7"
  }
}
